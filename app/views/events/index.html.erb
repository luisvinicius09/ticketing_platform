<p id="notice"><%= notice %></p>

<%= new_event(user_signed_in?) %>

<br/ >
<hr style="position: relative; top: 5px;"/>
<h2>Previous Events</h2>
<div class="grid">
  <%= render @prev_events %>
</div>

<h2>Upcoming Events</h2>
<div class="grid">
  <%= render @next_events %>
</div>